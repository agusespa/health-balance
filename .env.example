# --- Server Configuration ---
PORT=8080
DB_PATH=/app/data/health.db

# --- Web Push Notifications (Optional) ---
# Generate these keys to enable weekly reminders
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
